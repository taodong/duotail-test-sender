spring:
  application:
    name: duotail-test-email-sender
  mail:
    host: ${mail-host:localhost}
    port: ${mail-port:25}
    properties:
      mail:
        smtp:
          auth: ${mail-smtp-auth:false}
          starttls:
            enable: ${mail-smtp-starttls-enable:false}

app:
  permissions: ${permissions-file:classpath:permissions.yaml}

logging:
  file:
    path: ${LOG_DIR:.}
